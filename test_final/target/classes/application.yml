
server:
  port: 8080
spring:
  application:
    name: test_final
  datasource:
    url: jdbc:mysql://localhost:3306/order_manager?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root
  jpa:
    #    show-sql: true
    #      generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
